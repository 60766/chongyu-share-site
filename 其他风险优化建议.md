# 🔧 其他风险优化建议（不包含网络超时）

**检查时间**: 2025年1月  
**排除项**: 网络请求超时设置（保持5分钟）

---

## 🔴 高优先级风险（建议立即修复）

### 1. 数据持久化失败静默处理

**位置**: `ViewModels/PostViewModel.swift:303-316, 321-336`

**问题**:
```swift
do {
    let encoder = JSONEncoder()
    let data = try encoder.encode(sortedUserPosts)
    UserDefaults.standard.set(data, forKey: userPostsKey)
} catch {
    // 保存失败，静默处理 ❌
}
```

**风险**:
- 用户数据可能丢失，但用户不知道
- 没有错误提示，用户可能重复操作
- 可能导致数据不一致

**建议修复**:
```swift
do {
    let encoder = JSONEncoder()
    encoder.dateEncodingStrategy = .iso8601
    let data = try encoder.encode(sortedUserPosts)
    UserDefaults.standard.set(data, forKey: userPostsKey)
    #if DEBUG
    debugLog("✅ 用户帖子保存成功: \(sortedUserPosts.count) 条")
    #endif
} catch {
    #if DEBUG
    debugLog("❌ 用户帖子保存失败: \(error.localizedDescription)")
    #endif
    // ⚡️ 优化：保存失败时至少记录日志，生产环境可以考虑提示用户
    // 可以考虑添加重试机制或使用更可靠的存储方案
}
```

**修复优先级**: 🔴 高

---

### 2. 余额查询失败处理不友好

**位置**: `Services/WalletService.swift:111-120`

**问题**:
```swift
func getBalance() async throws -> Int {
    // ...
    guard let http = resp as? HTTPURLResponse, http.statusCode == 200 else {
        throw URLError(.badServerResponse)  // ❌ 错误信息不明确
    }
    let json = try JSONSerialization.jsonObject(with: data) as? [String: Any]
    return json?["balance"] as? Int ?? 0  // ❌ 如果解析失败返回0，可能误导用户
}
```

**风险**:
- 余额显示错误（显示0但实际有余额）
- 用户可能误以为余额为0而重复充值
- 影响用户体验和购买决策

**建议修复**:
```swift
func getBalance() async throws -> Int {
    let req = makeRequest(path: "api/balance", method: "GET")
    let session = createSession()
    
    do {
        let (data, resp) = try await session.data(for: req)
        guard let http = resp as? HTTPURLResponse else {
            throw NSError(domain: "wallet", code: -1, userInfo: [
                NSLocalizedDescriptionKey: "无法获取服务器响应"
            ])
        }
        
        guard http.statusCode == 200 else {
            let bodyText = String(data: data, encoding: .utf8) ?? ""
            throw NSError(domain: "wallet", code: http.statusCode, userInfo: [
                NSLocalizedDescriptionKey: "余额查询失败(\(http.statusCode)): \(bodyText.isEmpty ? "未知错误" : bodyText)"
            ])
        }
        
        guard let json = try JSONSerialization.jsonObject(with: data) as? [String: Any],
              let balance = json["balance"] as? Int else {
            throw NSError(domain: "wallet", code: -2, userInfo: [
                NSLocalizedDescriptionKey: "余额数据格式错误"
            ])
        }
        
        return balance
    } catch {
        // ⚡️ 优化：重新抛出更明确的错误
        throw error
    }
}
```

**修复优先级**: 🔴 高

---

### 3. Apple登录错误处理不完整

**位置**: `Services/AppleSignInManager.swift:43-49`

**问题**:
```swift
provider.getCredentialState(forUserID: appleID) { [weak self] state, error in
    DispatchQueue.main.async {
        if let error = error {
            #if DEBUG
            debugLog("🍎 getCredentialState error: \(error)")
            #endif
            // ❌ 只记录日志，没有用户提示或重试机制
        }
        // ...
    }
}
```

**风险**:
- 网络问题或Apple服务异常时，用户可能无法登录
- 没有友好的错误提示
- 没有重试机制

**建议修复**:
```swift
provider.getCredentialState(forUserID: appleID) { [weak self] state, error in
    DispatchQueue.main.async {
        if let error = error {
            #if DEBUG
            debugLog("🍎 getCredentialState error: \(error)")
            #endif
            
            // ⚡️ 优化：根据错误类型处理
            if let urlError = error as? URLError {
                switch urlError.code {
                case .notConnectedToInternet, .networkConnectionLost:
                    // 网络错误，可以考虑显示提示或自动重试
                    #if DEBUG
                    debugLog("🍎 网络错误，3秒后重试...")
                    #endif
                    DispatchQueue.main.asyncAfter(deadline: .now() + 3) {
                        self?.checkAppleSignInStatus()  // 自动重试
                    }
                default:
                    // 其他错误，至少记录
                    break
                }
            }
        }
        // ... 原有逻辑
    }
}
```

**修复优先级**: 🟡 中

---

## 🟡 中优先级风险

### 4. 内购错误提示可以更详细

**位置**: `Views/Profile/PurchaseView.swift:446`

**当前状态**: ✅ 已有错误处理，但可以优化

**问题**:
- `friendlyErrorMessage` 方法可能没有覆盖所有错误类型
- 某些错误信息可能不够友好

**建议**:
- 检查 `friendlyErrorMessage` 方法是否覆盖所有StoreKit错误
- 添加更详细的错误分类和提示
- 考虑添加重试机制

**修复优先级**: 🟡 中

---

### 5. 图片加载失败处理

**位置**: `Utils/ImageManager.swift:92-125`

**问题**:
- 图片加载失败时返回 `nil`，但没有占位图
- 可能导致UI显示空白

**建议**:
- 添加默认占位图
- 显示加载失败提示
- 添加重试机制

**修复优先级**: 🟢 低

---

## 📋 修复优先级总结

### 立即修复（上线前）
1. ✅ **数据持久化失败处理** - 添加错误日志，考虑用户提示
2. ✅ **余额查询失败处理** - 改进错误信息，避免误导用户

### 尽快修复（1.1版本）
3. **Apple登录错误处理** - 添加重试机制和友好提示
4. **内购错误提示优化** - 完善错误分类和提示

### 后续优化（1.2版本）
5. **图片加载失败处理** - 添加占位图和重试

---

## 🎯 建议的修复顺序

1. **数据持久化**（最重要，影响数据安全）
2. **余额查询**（影响用户体验和收入）
3. **Apple登录**（影响用户登录体验）
4. **内购错误**（已有基础，只需优化）
5. **图片加载**（影响较小）

---

**注意**: 网络请求超时设置保持5分钟不变，符合要求。

