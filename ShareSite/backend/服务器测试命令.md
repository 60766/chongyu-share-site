# 服务器端测试命令 - 直接复制粘贴

你现在已经在服务器上了（`iZbp13q0phv2mgyqtz083lZ`），不需要再SSH。

## 第一步：添加缺失的环境变量

```bash
cd /var/www/chongyu-backend

# 备份当前配置
cp production.env production.env.backup_$(date +%Y%m%d_%H%M%S)

# 添加视觉API积分配置
cat >> production.env << 'EOF'

# 视觉API积分扣除配置
CREDITS_PER_DOUBAO_SEED_VISION=10
CREDITS_PER_DOUBAO_VISION_PRO=20
EOF

# 验证配置
echo "=== 当前环境配置 ==="
cat production.env
```

## 第二步：重启服务

```bash
pm2 restart chongyu-api
sleep 2
pm2 logs chongyu-api --lines 10
```

## 第三步：运行完整测试

```bash
echo "=== 视觉API积分扣除测试 ===" && \
echo "" && \
echo "1. 环境配置:" && \
grep 'CREDITS_PER' production.env && \
echo "" && \
echo "2. 创建测试账户:" && \
curl -s -X POST http://localhost:3000/api/wallet/init \
  -H 'Content-Type: application/json' \
  -d '{"appAccountToken":"vision-test-final"}' && \
echo "" && \
echo "" && \
echo "3. 充值5000积分:" && \
curl -s -X POST http://localhost:3000/api/wallet/balance \
  -H 'Content-Type: application/json' \
  -d '{"appAccountToken":"vision-test-final","amount":5000}' && \
echo "" && \
echo "" && \
echo "4. 测试 doubao-seed-vision (应扣10积分):" && \
curl -s -X POST http://localhost:3000/api/vision \
  -H 'Content-Type: application/json' \
  -H 'X-App-Account-Token: vision-test-final' \
  -d '{"model":"doubao-seed-1-6-vision-250815","messages":[{"role":"user","content":"你好"}],"max_tokens":50}' | head -c 300 && \
echo "" && \
sleep 2 && \
echo "" && \
echo "5. 查看积分 (预期: 4990):" && \
curl -s http://localhost:3000/api/wallet/balance?appAccountToken=vision-test-final && \
echo "" && \
echo "" && \
echo "6. 测试 doubao-vision-pro (应扣20积分):" && \
curl -s -X POST http://localhost:3000/api/vision \
  -H 'Content-Type: application/json' \
  -H 'X-App-Account-Token: vision-test-final' \
  -d '{"model":"doubao-vision-pro-32k","messages":[{"role":"user","content":"你好"}],"max_tokens":20}' | head -c 300 && \
echo "" && \
sleep 2 && \
echo "" && \
echo "7. 查看最终积分 (预期: 4970):" && \
curl -s http://localhost:3000/api/wallet/balance?appAccountToken=vision-test-final && \
echo "" && \
echo "" && \
echo "=== 测试完成 ===" && \
echo "预期积分变化: 5000 → 4990 → 4970"
```

## 如果测试失败，检查日志

```bash
# 查看最近的错误日志
pm2 logs chongyu-api --lines 50 --err

# 查看所有日志
pm2 logs chongyu-api --lines 100

# 检查进程状态
pm2 status
```

## 一键复制：完整流程（所有步骤合并）

```bash
cd /var/www/chongyu-backend && \
echo "=== 步骤1: 备份并添加环境变量 ===" && \
cp production.env production.env.backup_$(date +%Y%m%d_%H%M%S) && \
grep -q "CREDITS_PER_DOUBAO_SEED_VISION" production.env || cat >> production.env << 'EOF'

# 视觉API积分扣除配置
CREDITS_PER_DOUBAO_SEED_VISION=10
CREDITS_PER_DOUBAO_VISION_PRO=20
EOF
echo "✓ 环境变量配置完成" && \
echo "" && \
cat production.env && \
echo "" && \
echo "=== 步骤2: 重启服务 ===" && \
pm2 restart chongyu-api && \
sleep 3 && \
echo "✓ 服务已重启" && \
echo "" && \
echo "=== 步骤3: 开始测试 ===" && \
echo "" && \
echo "3.1 创建测试账户:" && \
curl -s -X POST http://localhost:3000/api/wallet/init -H 'Content-Type: application/json' -d '{"appAccountToken":"vt-final"}' && \
echo "" && \
echo "" && \
echo "3.2 充值5000积分:" && \
curl -s -X POST http://localhost:3000/api/wallet/balance -H 'Content-Type: application/json' -d '{"appAccountToken":"vt-final","amount":5000}' && \
echo "" && \
echo "" && \
echo "3.3 调用seed模型 (扣10积分):" && \
curl -s -X POST http://localhost:3000/api/vision -H 'Content-Type: application/json' -H 'X-App-Account-Token: vt-final' -d '{"model":"doubao-seed-1-6-vision-250815","messages":[{"role":"user","content":"你好"}],"max_tokens":50}' | head -c 200 && \
echo "" && \
sleep 2 && \
echo "" && \
echo "3.4 查看余额 (应为4990):" && \
curl -s http://localhost:3000/api/wallet/balance?appAccountToken=vt-final && \
echo "" && \
echo "" && \
echo "3.5 调用pro模型 (扣20积分):" && \
curl -s -X POST http://localhost:3000/api/vision -H 'Content-Type: application/json' -H 'X-App-Account-Token: vt-final' -d '{"model":"doubao-vision-pro-32k","messages":[{"role":"user","content":"你好"}],"max_tokens":20}' | head -c 200 && \
echo "" && \
sleep 2 && \
echo "" && \
echo "3.6 查看最终余额 (应为4970):" && \
curl -s http://localhost:3000/api/wallet/balance?appAccountToken=vt-final && \
echo "" && \
echo "" && \
echo "=== 测试完成 ===" && \
echo "✓ 预期结果: 5000 → 4990 → 4970"
```

## 注意事项

1. **你已经在服务器上**：你的提示符 `[root@iZbp13q0phv2mgyqtz083lZ chongyu-backend]#` 表明你已经在服务器的正确目录了
2. **不要SSH到172.24.42.243**：这是另一台机器，密码也不对
3. **直接运行上面的命令**：在当前终端直接粘贴执行即可

## 测试结果说明

- **如果看到** `{"balance":4990}` 和 `{"balance":4970}` → ✅ 测试成功！
- **如果看到** `{"balance":5000}` 不变 → ❌ 积分未扣除，检查代码
- **如果看到** 错误信息 → 查看 `pm2 logs chongyu-api` 了解原因

