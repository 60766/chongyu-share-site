#!/usr/bin/expect -f

set timeout 30
set password "3Qq123456."

spawn ssh root@121.40.184.29
expect {
    "password:" {
        send "$password\r"
    }
    timeout {
        puts "连接超时"
        exit 1
    }
}

expect "#"
send "pm2 flush chongyu-backend\r"
expect "#"
send "echo '========== 日志已清空，准备监听新请求 =========='\r"
expect "#"
send "exit\r"
expect eof

