# 真实购买验证结果分析

**检查时间：** 2025-12-03  
**交易 ID：** 2, 3, 4

---

## ✅ 购买成功

从交易记录可以看到，3次购买都成功了：
- ✅ 产品 ID: `com.lishilong.chongyu.100energy`
- ✅ 充值成功（余额正确增加）
- ✅ Receipt 已保存到交易记录

---

## ⚠️ 验证失败原因分析

### 问题发现

从交易记录中的验证信息可以看到：

```json
"verification": {
  "valid": false,
  "error": "jwt_verification_failed",
  "warning": {
    "error": "jwt_verification_failed",
    "reason": "Invalid Compact JWS",
    "strictMode": false
  }
}
```

### 根本原因

**Receipt 格式问题：**

从交易记录可以看到，receipt 是 **JSON 对象格式**，而不是 **JWT 字符串格式**：

```json
"receipt": {
  "appTransactionId": "0",
  "storefrontId": "143441",
  "type": "Consumable",
  "signedDate": 1764699016668,
  "bundleId": "com.lishilong.chongyu",
  "transactionId": "2",
  "productId": "com.lishilong.chongyu.100energy",
  ...
}
```

**但是验证函数期望的是 JWT 字符串格式：**
```
eyJhbGciOiJSUzI1NiIsIng1YyI6WyJ...（很长的字符串）
```

---

## 🔍 代码检查

### iOS 端代码

在 `StoreKitManager.swift` 中：

```swift
let receiptJSON: String? = String(data: transaction.jsonRepresentation, encoding: .utf8)
```

`transaction.jsonRepresentation` 返回的是 **JWT 字符串**，但可能在传输过程中被解析成了 JSON 对象。

### 后端代码

在 `server.js` 中：

```javascript
// Optional: keep parsed receipt fields for audit
let parsedReceipt = null
try { 
  if (typeof receipt === 'string' && receipt.length) {
    parsedReceipt = JSON.parse(receipt)
  }
} catch {}
```

这里尝试解析 receipt，但如果 receipt 已经是对象，解析会失败或产生意外结果。

---

## 💡 解决方案

### 方案1：检查 iOS 端发送的 receipt 格式

需要确认 iOS 端发送的 `receipt` 是否是原始的 JWT 字符串。

**检查方法：**
1. 在 iOS 代码中添加日志，打印 `receiptJSON` 的内容
2. 确认是否是 JWT 格式（`eyJ...` 开头）

### 方案2：修改后端代码，支持两种格式

如果 iOS 端发送的是解析后的 JSON 对象，需要修改后端代码：

```javascript
// 如果 receipt 是对象，尝试提取 JWT
if (typeof receipt === 'object' && receipt !== null) {
  // 可能是解析后的 JSON，需要找到原始的 JWT
  // 或者使用其他方式验证
}
```

### 方案3：使用 App Store Server API 验证

如果 receipt 是 JSON 对象格式，可以使用 App Store Server API 的其他验证方式。

---

## 📋 当前状态

| 项目 | 状态 | 说明 |
|------|------|------|
| **购买功能** | ✅ 正常 | 购买成功，充值正常 |
| **Receipt 提供** | ✅ 正常 | Receipt 已提供并保存 |
| **Receipt 格式** | ⚠️ 问题 | 是 JSON 对象，不是 JWT 字符串 |
| **验证功能** | ❌ 失败 | 因为格式不匹配而失败 |
| **非严格模式** | ✅ 正常 | 验证失败但继续处理（符合预期） |

---

## 🎯 下一步行动

### 1. 检查 iOS 端代码

确认 `transaction.jsonRepresentation` 返回的是什么格式：

```swift
let receiptJSON: String? = String(data: transaction.jsonRepresentation, encoding: .utf8)
print("[IAP] Receipt format: \(receiptJSON?.prefix(50) ?? "nil")")
```

### 2. 如果 receipt 是 JWT 字符串

- 检查传输过程中是否被解析
- 确保后端收到的是原始 JWT 字符串

### 3. 如果 receipt 是 JSON 对象

- 需要修改验证逻辑
- 或者使用 App Store Server API 的其他验证方式

---

## ✅ 结论

**购买功能正常：**
- ✅ 购买请求成功
- ✅ 充值成功
- ✅ Receipt 已保存

**验证功能需要修复：**
- ⚠️ Receipt 格式不匹配（JSON 对象 vs JWT 字符串）
- ⚠️ 需要检查 iOS 端发送的格式
- ⚠️ 可能需要调整验证逻辑

**当前影响：**
- 非严格模式下，验证失败但继续处理
- 充值功能正常
- 但无法验证交易真实性

**建议：**
1. 检查 iOS 端发送的 receipt 格式
2. 根据实际情况调整验证逻辑
3. 确保能够验证真实的 JWT receipt

