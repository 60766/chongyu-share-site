# 测试脚本使用说明

## 📍 执行位置说明

测试脚本可以在**两个地方**执行：

### 方式1：在本地电脑执行（推荐）✅

**优点：**
- ✅ 不需要 SSH 到服务器
- ✅ 可以测试生产环境 API
- ✅ 方便调试和查看结果

**使用方法：**
```bash
# 在本地电脑的终端执行
cd /Users/lishilong/IOS开发/虫遇/虫遇

# 测试本地开发服务器
./test_iap_verification.sh

# 测试远程生产服务器
SERVER=https://api.chongyuai.com ./test_iap_verification.sh
```

**前提条件：**
- 本地电脑需要能访问服务器（网络连通）
- 服务器需要开放 API 端口（3000 或 443）

---

### 方式2：在服务器上执行

**优点：**
- ✅ 直接访问本地 API，延迟更低
- ✅ 不需要考虑网络问题

**使用方法：**
```bash
# SSH 到服务器
ssh root@your-server-ip

# 进入项目目录
cd /var/www/chongyu-backend  # 或你的后端目录

# 执行测试脚本
./test_iap_verification.sh
```

**前提条件：**
- 需要 SSH 访问权限
- 服务器上需要有测试脚本文件

---

## 🎯 推荐方式

### 开发/测试环境
**在本地电脑执行**，测试本地开发服务器：
```bash
SERVER=http://localhost:3000 ./test_iap_verification.sh
```

### 生产环境
**在本地电脑执行**，测试生产服务器：
```bash
SERVER=https://api.chongyuai.com ./test_iap_verification.sh
```

---

## 📋 完整测试流程

### 步骤1：在本地电脑准备测试脚本

```bash
# 1. 确保脚本有执行权限
chmod +x test_iap_verification.sh

# 2. 检查脚本内容
cat test_iap_verification.sh | head -20
```

### 步骤2：测试本地开发服务器

```bash
# 确保本地服务器正在运行
# 在另一个终端运行：
cd backend
node server.js

# 然后执行测试
./test_iap_verification.sh
```

### 步骤3：测试生产服务器

```bash
# 直接测试生产环境
SERVER=https://api.chongyuai.com ./test_iap_verification.sh

# 如果需要指定 token
SERVER=https://api.chongyuai.com TOKEN=your_token ./test_iap_verification.sh
```

---

## 🔍 查看日志

### 如果脚本在本地执行

**查看服务器日志（需要 SSH）：**
```bash
# SSH 到服务器
ssh root@your-server-ip

# 查看日志
tail -f /var/log/chongyu-backend.log | grep -E "IAP|Verify"
```

**或者使用 PM2（如果服务器使用 PM2）：**
```bash
ssh root@your-server-ip "pm2 logs chongyu-backend | grep -E 'IAP|Verify'"
```

### 如果脚本在服务器执行

**直接查看日志：**
```bash
# 在服务器上直接查看
tail -f /var/log/chongyu-backend.log | grep -E "IAP|Verify"
```

---

## 🛠️ 常见问题

### Q1: 脚本在本地执行，但无法连接到服务器？

**检查：**
```bash
# 1. 检查网络连接
ping api.chongyuai.com

# 2. 检查端口是否开放
curl -I https://api.chongyuai.com/health

# 3. 检查防火墙设置
```

### Q2: 脚本在服务器执行，但找不到文件？

**解决：**
```bash
# 1. 上传脚本到服务器
scp test_iap_verification.sh root@your-server-ip:/var/www/chongyu-backend/

# 2. 或直接在服务器上创建
ssh root@your-server-ip
cd /var/www/chongyu-backend
# 然后创建脚本文件
```

### Q3: 权限不足？

**解决：**
```bash
# 添加执行权限
chmod +x test_iap_verification.sh

# 如果还是不行，使用 bash 直接执行
bash test_iap_verification.sh
```

---

## 📝 快速参考

| 场景 | 执行位置 | 命令 |
|------|---------|------|
| 本地开发测试 | 本地电脑 | `SERVER=http://localhost:3000 ./test_iap_verification.sh` |
| 生产环境测试 | 本地电脑 | `SERVER=https://api.chongyuai.com ./test_iap_verification.sh` |
| 服务器本地测试 | 服务器 | `./test_iap_verification.sh` |

---

## 💡 最佳实践

1. **开发阶段**：在本地电脑测试本地服务器
2. **部署前**：在本地电脑测试生产服务器
3. **问题排查**：在服务器上直接查看日志

