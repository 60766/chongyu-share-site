# 角色聊天画像功能说明

## 📖 功能概述

基于用户与单个虚拟角色的一对一私聊，使用 AI 生成有趣的互动画像。这是一个轻量级的小功能，让用户了解自己的聊天风格和与角色的关系。

## 🎯 核心价值

### 用户心理分析（第一性原理）

用户使用虫遇 App 的核心心理需求：
1. **被理解的渴望** - "有人懂我"
2. **智慧的渴求** - "我想变聪明"
3. **陪伴的需要** - "我不想孤独"
4. **表达的欲望** - "我有话要说"
5. **成长的满足感** - "我在进步"

### 用户真正想知道的

- **"我是什么样的人？"**
- **"我和谁最聊得来？"**
- **"我最近在想什么？"**
- **"我有什么变化？"**

## 🎨 画像内容

每个画像包含以下元素：

1. **画像标题**（≤12字）
   - 简短有趣，如"深夜思考家"、"好奇宝宝"、"话痨搭档"

2. **一句话总结**（80-120字）
   - 轻松自然，像朋友说话
   - 描述用户的聊天风格和与角色的关系

3. **个性标签**（3个）
   - 词或短语，描述用户特点
   - 如"文艺倾向"、"简洁表达"、"夜间活跃"

4. **精彩片段**（2-3条）
   - 从真实对话中摘录的原话
   - 展示用户或角色的代表性发言

5. **最近关注点**（1-2句话）
   - 概括最近几次对话的主题

6. **聊天建议**（1句话）
   - 给用户一个有趣的下次聊天建议

## 🔧 技术实现

### 核心文件

1. **数据模型**：`Models/CharacterChatInsight.swift`
   - `CharacterChatInsight`：画像数据结构
   - `HighlightQuote`：精彩片段
   - `CharacterChatInsightCache`：缓存模型（SwiftData）

2. **服务层**：`Services/CharacterChatInsightService.swift`
   - 数据获取：从 SwiftData 获取聊天消息
   - AI 调用：使用 `AINetworkService` 生成画像
   - 缓存管理：智能缓存，避免重复生成

3. **视图层**：`Views/CharacterChatInsightView.swift`
   - 展示画像卡片
   - 加载状态和错误处理
   - 刷新功能

### 数据流程

```
用户请求
  ↓
检查缓存（24小时内 + 新消息<10条）
  ↓
获取聊天消息（近30天，最多120轮对话）
  ↓
构建 AI 提示词
  ↓
调用 AI API
  ↓
解析 JSON 响应
  ↓
验证引用真实性
  ↓
保存到缓存
  ↓
展示画像
```

### AI 提示词设计

**核心原则**：
- 只依据提供的对话，不得编造
- 不得引用不存在的句子
- 输出严格 JSON 格式
- 语言轻松自然

**输出格式**：
```json
{
  "title": "深夜思考家",
  "summary": "你常在深夜和莎士比亚聊创作与意义，表达克制但问题直接。",
  "tags": ["文艺倾向", "简洁表达", "夜间活跃"],
  "highlights": [
    {"quote": "我想了解更多关于你的思想", "from": "你"},
    {"quote": "对现代科学你怎么看？", "from": "你"}
  ],
  "recentFocus": "持续围绕创作与现代科学价值碰撞展开。",
  "nextSuggestion": "向他请教"如何把个人困惑写成作品"的具体练习法。"
}
```

## 📱 使用方式

### 在代码中调用

```swift
import SwiftUI
import SwiftData

struct SomeView: View {
    @Environment(\.modelContext) private var modelContext
    
    var body: some View {
        NavigationLink {
            CharacterChatInsightView(
                characterId: "shakespeare",
                characterName: "莎士比亚"
            )
        } label: {
            Text("查看互动画像")
        }
    }
}
```

### 直接使用服务

```swift
import SwiftData

// 生成画像
CharacterChatInsightService.shared.generateInsight(
    characterId: "shakespeare",
    characterName: "莎士比亚",
    modelContext: modelContext
) { result in
    switch result {
    case .success(let insight):
        print("标题: \(insight.title)")
        print("总结: \(insight.summary)")
    case .failure(let error):
        print("错误: \(error.localizedDescription)")
    }
}

// 清除缓存
CharacterChatInsightService.shared.clearCache(
    characterId: "shakespeare",
    modelContext: modelContext
)
```

## 🎯 集成建议

### 推荐位置

1. **角色详情页**
   - 在聊天记录上方添加"互动画像"入口
   - 显示简化版卡片，点击查看详情

2. **聊天界面**
   - 在聊天界面顶部添加"画像"按钮
   - 长按角色头像显示快捷菜单

3. **个人中心**
   - "我的互动画像"列表
   - 展示所有聊过的角色的画像

### UI 集成示例

```swift
// 在角色详情页添加入口
struct CharacterDetailView: View {
    let character: CharacterModel
    @Environment(\.modelContext) private var modelContext
    
    var body: some View {
        ScrollView {
            VStack {
                // 角色信息...
                
                // 互动画像入口
                NavigationLink {
                    CharacterChatInsightView(
                        characterId: character.id,
                        characterName: character.name
                    )
                } label: {
                    HStack {
                        Image(systemName: "sparkles")
                        Text("查看互动画像")
                        Spacer()
                        Image(systemName: "chevron.right")
                    }
                    .padding()
                    .background(Color.blue.opacity(0.1))
                    .cornerRadius(12)
                }
                
                // 其他内容...
            }
        }
    }
}
```

## ⚙️ 配置参数

### 可调整参数

```swift
// 在 CharacterChatInsightService.swift 中

// 限制天数（默认30天）
limitDays: Int = 30

// 最大对话轮数（默认120轮）
maxTurns: Int = 120

// 最小消息数量（默认10条）
minMessageCount: Int = 10

// 缓存过期时间（默认24小时）
cacheExpirationHours: Double = 24

// 触发刷新的新消息数量（默认10条）
refreshThreshold: Int = 10
```

## 🔒 隐私与安全

1. **数据范围**
   - 只发送该角色的对话消息
   - 不包含其他角色的聊天内容

2. **内容过滤**
   - 消息内容超过120字会截断
   - 保护用户隐私信息

3. **引用验证**
   - AI 生成的引用必须存在于真实对话中
   - 无效引用会被自动过滤

## 💰 成本控制

1. **智能缓存**
   - 24小时内不重复生成
   - 新消息少于10条不刷新

2. **消息限制**
   - 最多处理120轮对话
   - 单条消息最多120字

3. **API 优化**
   - `max_tokens`: 800
   - `temperature`: 0.7
   - 不使用流式传输

## 🐛 错误处理

### 常见错误及处理

1. **数据不足**
   - 提示：和角色多聊聊吧～
   - 最少需要10条对话

2. **JSON 解析失败**
   - 自动尝试修复一次
   - 修复失败则提示重试

3. **网络错误**
   - 显示错误信息
   - 提供重试按钮

4. **缓存错误**
   - 静默失败，不影响功能
   - 下次会重新生成

## 📊 数据统计

### 可追踪指标

- 生成次数
- 成功率
- 平均响应时间
- 缓存命中率
- 用户刷新频率

## 🚀 未来优化

1. **多角色对比**
   - 横向对比与不同角色的互动风格

2. **时间线变化**
   - 展示画像随时间的演变

3. **社交分享**
   - 生成精美的分享卡片

4. **个性化推荐**
   - 基于画像推荐相似角色

## 📝 注意事项

1. **首次使用**
   - 需要至少10条对话才能生成
   - 建议与角色深度交流后再查看

2. **刷新频率**
   - 不建议频繁刷新（消耗穿越能量）
   - 有明显变化时再重新生成

3. **数据准确性**
   - AI 生成内容仅供参考
   - 基于有限的对话数据

## 🎉 总结

这是一个**简单、有趣、有用**的小功能：

- ✅ **满足好奇心**："原来我是这样的人！"
- ✅ **增加粘性**："我要继续聊天看看画像会怎么变"
- ✅ **社交分享**："快看我的虫遇画像！"
- ✅ **个性化推荐**：基于画像推荐合适的角色和话题

**核心就是让用户觉得："哇，这个 App 真的懂我！"** 🎯

