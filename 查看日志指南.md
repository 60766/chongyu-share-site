# 查看服务器 IAP 验证日志指南

## 🔍 查看日志的方法

由于无法直接 SSH 访问服务器，请按照以下步骤手动查看日志：

---

## 方法1：SSH 到服务器查看 PM2 日志（推荐）

```bash
# 1. SSH 到服务器
ssh root@121.40.184.29

# 2. 查看最近的 IAP 相关日志
pm2 logs chongyu-backend --lines 100 | grep -E "IAP|Verify"

# 或查看所有日志（实时）
pm2 logs chongyu-backend | grep -E "IAP|Verify"
```

**预期看到的日志：**
```
[IAP] confirm payload { appAccountToken: 'xxx', productId: 'xxx', transactionId: 'xxx', hasReceipt: true }
[IAP Verify] ✅ 交易验证成功 { productId: 'xxx', transactionId: 'xxx' }
[IAP Verify] ⚠️ 未提供 receipt，无法验证交易真实性
[IAP Verify] ⚠️ 验证失败但继续处理（非严格模式）
[Config] IAP_VERIFY_STRICT: disabled (compatibility mode)
```

---

## 方法2：查看日志文件

```bash
# 1. SSH 到服务器
ssh root@121.40.184.29

# 2. 查看日志文件
tail -200 /var/log/chongyu-backend.log | grep -E "IAP|Verify"

# 或实时查看
tail -f /var/log/chongyu-backend.log | grep -E "IAP|Verify"
```

---

## 方法3：查看最近的交易记录

```bash
# 1. SSH 到服务器
ssh root@121.40.184.29

# 2. 进入后端目录
cd /var/www/chongyu-backend

# 3. 查看最近的 IAP 交易（包含验证信息）
cat server-data.json | jq '.transactions[] | select(.meta.productId != null) | {
  id: .id,
  type: .type,
  amount: .amount,
  ref: .ref,
  productId: .meta.productId,
  verification: .meta.verification,
  at: .at
}' | tail -20
```

**预期看到的验证信息：**
```json
{
  "id": "xxx",
  "type": "topup",
  "amount": 1800,
  "ref": "2000000123456789",
  "productId": "com.lishilong.chongyu.100energy",
  "verification": {
    "valid": true,
    "error": null,
    "warning": null
  },
  "at": 1764698773000
}
```

---

## 方法4：检查配置

```bash
# 1. SSH 到服务器
ssh root@121.40.184.29

# 2. 检查 IAP 验证配置
cd /var/www/chongyu-backend
grep IAP_VERIFY_STRICT production.env

# 3. 检查代码是否包含验证函数
grep -n "verifyIAPTransaction" server.js
```

---

## 📋 需要检查的关键信息

### 1. 配置检查
- [ ] `IAP_VERIFY_STRICT` 的值（0 或 1）
- [ ] 启动日志中是否有 `[Config] IAP_VERIFY_STRICT` 输出

### 2. 验证日志检查
- [ ] 是否有 `[IAP Verify]` 开头的日志
- [ ] 验证成功时是否有 `✅ 交易验证成功`
- [ ] 验证失败时是否有 `⚠️ 验证失败` 或 `❌ JWT verification failed`

### 3. 交易记录检查
- [ ] 交易记录中的 `meta.verification` 字段
- [ ] 是否有验证相关的警告信息

---

## 🔍 常见日志示例

### 验证成功的日志
```
[IAP] confirm payload { appAccountToken: 'xxx', productId: 'com.lishilong.chongyu.100energy', transactionId: '2000000123456789', hasReceipt: true }
[IAP Verify] ✅ 交易验证成功 { productId: 'com.lishilong.chongyu.100energy', transactionId: '2000000123456789', purchaseDate: 1234567890 }
```

### 验证失败的日志（非严格模式）
```
[IAP] confirm payload { appAccountToken: 'xxx', productId: 'xxx', transactionId: 'xxx', hasReceipt: true }
[IAP Verify] ❌ JWT verification failed: invalid JWT
[IAP Verify] ⚠️ 验证出错但继续处理（非严格模式）
```

### 未提供 receipt 的日志
```
[IAP] confirm payload { appAccountToken: 'xxx', productId: 'xxx', transactionId: 'xxx', hasReceipt: false }
[IAP Verify] ⚠️ 未提供 receipt，无法验证交易真实性
```

---

## 🛠️ 如果看不到验证日志

### 可能的原因：
1. **代码未部署**：服务器上的代码可能还是旧版本
2. **配置问题**：可能没有正确加载环境变量
3. **日志位置不同**：日志可能在其他位置

### 排查步骤：

```bash
# 1. 检查代码版本
ssh root@121.40.184.29 "cd /var/www/chongyu-backend && grep -n 'verifyIAPTransaction' server.js"

# 2. 检查配置
ssh root@121.40.184.29 "cd /var/www/chongyu-backend && cat production.env | grep IAP"

# 3. 检查服务是否重启
ssh root@121.40.184.29 "pm2 list"

# 4. 查看所有日志（不筛选）
ssh root@121.40.184.29 "pm2 logs chongyu-backend --lines 50 --nostream | tail -50"
```

---

## 📝 快速命令参考

```bash
# 查看最近的 IAP 日志
ssh root@121.40.184.29 "pm2 logs chongyu-backend --lines 100 --nostream | grep -E 'IAP|Verify' | tail -30"

# 查看配置
ssh root@121.40.184.29 "cd /var/www/chongyu-backend && grep IAP_VERIFY_STRICT production.env"

# 查看最近的交易
ssh root@121.40.184.29 "cd /var/www/chongyu-backend && cat server-data.json | jq '.transactions[-5:]'"
```

---

## 💡 提示

如果 SSH 需要密码，系统会提示你输入。输入密码后即可查看日志。

如果无法 SSH，可以：
1. 使用其他有权限的账号
2. 联系服务器管理员
3. 通过其他方式访问日志（如日志收集系统）

