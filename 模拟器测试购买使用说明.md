# 模拟器测试购买功能使用说明

## 📋 功能说明

在 Xcode 模拟器中测试应用内购买时，如果 StoreKit 配置文件无法正常加载产品，应用会自动启用**模拟器测试模式**，允许你直接测试购买功能，无需真实支付。

---

## 🎯 使用方法

### 方法1：使用 StoreKit 配置文件（推荐）

1. **确保 StoreKit 配置文件已配置**
   - 文件位置：`虫遇/StoreKit.storekit`
   - 已在 Scheme 中正确引用

2. **在 Xcode 中运行应用**
   - 选择模拟器设备
   - 运行应用（⌘R）

3. **如果产品正常加载**
   - 会显示真实的购买选项
   - 点击购买会弹出 StoreKit 测试界面
   - 无需真实支付，直接完成购买

### 方法2：使用模拟器测试模式（备用方案）

如果 StoreKit 产品加载失败（例如配置文件未正确加载），应用会自动启用测试模式：

1. **打开充值页面**
   - 在应用中点击"充值"或"虫洞币"按钮

2. **查看测试模式界面**
   - 会显示"模拟器测试模式"提示
   - 显示4个测试购买选项：
     - 1800虫洞币（¥6）
     - 6000虫洞币（¥18）
     - 13800虫洞币（¥38）
     - 24000虫洞币（¥68）

3. **点击任意套餐进行测试**
   - 点击任意套餐按钮
   - 系统会直接调用后端API完成购买
   - 无需弹出支付界面
   - 购买成功后显示成功提示

---

## 🔧 技术实现

### 工作原理

1. **检测环境**
   - 应用启动时检测是否在模拟器中运行
   - 尝试加载 StoreKit 产品

2. **启用测试模式**
   - 如果产品加载失败，设置 `isSimulatorFallback = true`
   - UI 显示测试购买按钮

3. **模拟购买流程**
   - 生成模拟交易ID：`simulator-test-{UUID}`
   - 直接调用后端API：`WalletService.confirmPurchase()`
   - 绕过 StoreKit 验证
   - 更新余额并显示成功提示

### 代码位置

- **StoreKitManager.swift**: 产品加载和测试模式检测
- **PurchaseView.swift**: UI显示和购买逻辑
- **WalletService.swift**: 后端API调用

---

## ⚙️ 配置 StoreKit 文件

### 检查配置

1. **打开 Xcode Scheme**
   - Product → Scheme → Edit Scheme
   - 选择 Run → Options
   - 检查 "StoreKit Configuration" 是否指向 `StoreKit.storekit`

2. **验证 StoreKit 文件**
   - 文件路径：`虫遇/StoreKit.storekit`
   - 确保包含4个产品ID：
     - `com.lishilong.chongyu.100energy`
     - `com.lishilong.chongyu.300energy`
     - `com.lishilong.chongyu.700energy`
     - `com.lishilong.chongyu.1400energy`

### 如果 StoreKit 文件未正确加载

应用会自动回退到测试模式，你可以：
- 使用测试模式继续测试
- 或者修复 StoreKit 配置后重新运行

---

## 🐛 调试信息

### 查看日志

在 Xcode 控制台中查看以下日志：

```
[IAP] ℹ️ 当前运行在模拟器...
[IAP] ✅ 找到 StoreKit 配置文件: ...
[IAP] 🔄 开始请求产品信息...
[IAP] 成功加载 X 个商品
```

如果启用测试模式：
```
[IAP] 🔄 启用模拟器备用模式，创建本地模拟产品...
[IAP] ✅ 模拟器备用模式已启用，UI将显示测试充值按钮
```

测试购买时：
```
[IAP] ✅ 模拟器测试购买成功: com.lishilong.chongyu.100energy, 交易ID: simulator-test-xxx
[IAP] 💰 余额更新: 1800 虫洞币
```

---

## ⚠️ 注意事项

1. **仅限模拟器**
   - 测试模式仅在模拟器中可用
   - 真机上会使用真实的 StoreKit 购买流程

2. **DEBUG 模式**
   - 测试模式代码包含在 `#if DEBUG` 中
   - Release 版本不会包含此功能

3. **后端API**
   - 测试模式仍然会调用真实的后端API
   - 确保后端服务正常运行
   - 交易ID格式：`simulator-test-{UUID}`

4. **数据一致性**
   - 测试购买会真实更新数据库余额
   - 建议使用测试账号进行测试

---

## 📝 常见问题

### Q: 为什么看不到测试模式？

**A:** 检查以下几点：
1. 确保在模拟器中运行（不是真机）
2. 确保是 DEBUG 构建
3. 确保 StoreKit 产品加载失败（如果成功加载，会显示真实产品）

### Q: 测试购买失败怎么办？

**A:** 检查：
1. 网络连接是否正常
2. 后端服务是否运行
3. 是否已登录账号（需要 appAccountToken）
4. 查看控制台错误日志

### Q: 如何切换到真实 StoreKit 测试？

**A:** 
1. 确保 StoreKit.storekit 文件配置正确
2. 在 Scheme 中正确引用 StoreKit 文件
3. 重新运行应用
4. 如果产品加载成功，会自动使用 StoreKit

### Q: 测试购买会影响生产数据吗？

**A:** 
- 测试购买会调用真实的后端API
- 建议使用测试环境或测试账号
- 交易ID包含 `simulator-test-` 前缀，便于识别

---

## 🎉 完成

现在你可以在模拟器中方便地测试购买功能了！

如果遇到问题，请查看 Xcode 控制台的日志输出，或联系开发者。

