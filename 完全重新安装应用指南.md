# 完全重新安装应用 - 使用真实 Sandbox 环境

## 🔍 问题

即使禁用了 StoreKit Configuration，应用还是显示 Xcode 环境。这是因为应用没有完全重新安装。

## ✅ 解决步骤

### 方法 1：在 iPad 上删除应用后重新安装（推荐）

1. **在 iPad 上删除应用**：
   - 长按应用图标
   - 点击"删除 App"
   - 确认删除

2. **在 Xcode 中清理构建**：
   - 在 Xcode 中，按 `Cmd + Shift + K`（Product → Clean Build Folder）
   - 等待清理完成

3. **重新运行应用**：
   - 在 Xcode 中选择你的 iPad 设备
   - 点击运行按钮（▶️）
   - 等待应用完全安装

4. **验证配置**：
   - 在 Xcode 中：**Product → Scheme → Edit Scheme...**
   - 选择 **Run**
   - 确认 **StoreKit Configuration File** 显示为 **None** 或未勾选
   - 如果没有，手动取消勾选

### 方法 2：通过 Xcode 完全重新安装

1. **停止应用**：
   - 在 Xcode 中点击停止按钮（■）

2. **清理构建**：
   - 按 `Cmd + Shift + K`（Product → Clean Build Folder）

3. **删除派生数据**（可选但推荐）：
   - 在 Xcode 中：**Xcode → Settings → Locations**
   - 点击 Derived Data 路径旁边的箭头
   - 找到你的项目文件夹，删除它

4. **重新构建并运行**：
   - 在 Xcode 中选择你的 iPad 设备
   - 按 `Cmd + R` 运行
   - 等待完全安装

### 方法 3：验证 Scheme 配置

1. **打开 Scheme 编辑器**：
   - 在 Xcode 中：**Product → Scheme → Edit Scheme...**

2. **检查 Run 配置**：
   - 选择左侧的 **Run**
   - 查看右侧的 **StoreKit Configuration** 部分
   - 确保显示为 **None** 或未勾选任何文件

3. **如果还是显示 StoreKit.storekit**：
   - 点击下拉菜单，选择 **None**
   - 点击 **Close** 保存

---

## 🧪 验证是否成功

重新安装后，执行购买测试：

1. **确保已退出 Apple ID**（设置 → Apple ID → 退出登录）
2. **使用 Sandbox 账号购买**
3. **检查服务器日志**：
   ```bash
   ./check_latest_iap.sh
   ```

应该看到：
- ✅ `environment: 'Sandbox'`（不是 'Xcode'）
- ✅ JWT 格式的 receipt（不是 JSON 对象）
- ✅ `[IAP Verify] ✅ 交易验证成功`（JWT 格式）

---

## 🔧 如果还是不行

如果重新安装后还是显示 Xcode 环境，可能的原因：

1. **检查项目设置**：
   - 在 Xcode 中：**项目 → TARGETS → 虫遇 → Build Settings**
   - 搜索 "StoreKit"，确保没有硬编码的配置

2. **检查 Info.plist**：
   - 确保没有 StoreKit 相关的配置

3. **完全重启 Xcode**：
   - 退出 Xcode
   - 重新打开项目
   - 重新运行

4. **检查设备上的应用**：
   - 确保删除的是最新版本的应用
   - 如果有多个版本，全部删除

---

## 📝 注意事项

- **删除应用会清除所有数据**（如果应用有本地存储）
- **重新安装后需要重新登录**（如果有登录功能）
- **确保网络连接正常**（Sandbox 环境需要网络）

---

## 🎯 成功标志

如果一切正常，服务器日志应该显示：

```
[IAP Verify] ✅ 交易验证成功 {
  productId: 'com.lishilong.chongyu.100energy',
  transactionId: '15',
  environment: 'Sandbox'  // ← 这里应该是 'Sandbox'，不是 'Xcode'
}
```

receipt 应该是 JWT 格式（以 `eyJ` 开头），而不是 JSON 对象格式。

