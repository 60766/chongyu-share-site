# 上线前最终测试指南

## 📋 当前测试状态

### ✅ 已测试：Xcode 环境
- **Receipt 格式：** JSON 对象格式
- **验证结果：** ✅ 成功
- **环境：** Xcode（开发环境）

### ⚠️ 待测试：Sandbox/生产环境
- **Receipt 格式：** JWT 字符串格式（`eyJ...`）
- **验证方式：** JWT 签名验证
- **环境：** Sandbox（测试环境）或生产环境

---

## 🎯 为什么需要测试 Sandbox 环境？

### 不同环境的差异

| 环境 | Receipt 格式 | 验证方式 | 是否已测试 |
|------|-------------|---------|-----------|
| **Xcode** | JSON 对象 | 字段验证 | ✅ 已测试 |
| **Sandbox** | JWT 字符串 | JWT 签名验证 | ⚠️ 待测试 |
| **生产环境** | JWT 字符串 | JWT 签名验证 | ⚠️ 待测试 |

### 重要性

1. **生产环境使用 JWT 格式**
   - 真实用户购买时，receipt 是 JWT 字符串
   - 需要验证 JWT 签名，确保交易真实性

2. **验证逻辑不同**
   - Xcode：字段验证（检查 productId、transactionId）
   - Sandbox/生产：JWT 签名验证（更安全）

3. **确保功能完整**
   - 两种格式的验证都需要正常工作
   - 避免上线后出现问题

---

## 🧪 Sandbox 环境测试步骤

### 步骤1：创建 Sandbox 测试账号

1. 登录 [App Store Connect](https://appstoreconnect.apple.com)
2. 进入 **用户和访问** → **沙盒技术测试员**
3. 点击 **+** 添加测试账号
4. 填写信息：
   - 邮箱：`sandbox-test@example.com`
   - 密码：`Test123456`
   - 国家：中国
   - 姓名：Sandbox测试

### 步骤2：在真机上测试

**重要：必须使用真机，不能使用模拟器！**

1. **使用真机**（iPhone/iPad）
2. **不要**在设置中登录 Apple ID
3. 通过 Xcode 安装应用（或使用 TestFlight）
4. 打开应用 → 充值页面 → 点击购买
5. **弹出登录时，输入 Sandbox 测试账号**
6. 完成购买流程

### 步骤3：检查验证结果

```bash
ssh root@121.40.184.29
pm2 logs chongyu-backend --lines 50 | grep -E "IAP|Verify"
```

**预期看到的日志：**

```
[IAP] confirm payload {
  receiptType: 'string',
  receiptPreview: 'eyJhbGciOiJSUzI1NiIsIng1YyI6WyJ...'  ← JWT 格式
}
[IAP Verify] ✅ 交易验证成功 {
  productId: 'com.lishilong.chongyu.100energy',
  transactionId: '2000000123456789',
  purchaseDate: 1234567890
}
```

**交易记录中的验证信息：**

```json
"verification": {
  "valid": true,
  "format": "jwt",
  "warning": null
}
```

---

## 🔍 验证要点

### 1. Receipt 格式检查

**Sandbox 环境应该看到：**
- `receiptType: 'string'`
- `receiptPreview: 'eyJ...'`（JWT 格式，以 `eyJ` 开头）

**Xcode 环境看到的是：**
- `receiptType: 'string'`
- `receiptPreview: '{"appTransactionId":"0",...'`（JSON 格式）

### 2. 验证日志检查

**Sandbox 环境应该看到：**
```
[IAP Verify] ✅ 交易验证成功 {
  productId: 'xxx',
  transactionId: 'xxx',
  purchaseDate: xxx
}
```

**不应该看到：**
- `[IAP Verify] ℹ️ Receipt 是 JSON 字符串格式`（这是 Xcode 环境的日志）
- `[IAP Verify] ✅ 交易验证成功（对象格式）`（这是对象格式的日志）

### 3. 验证结果检查

**Sandbox 环境应该看到：**
```json
"verification": {
  "valid": true,
  "format": "jwt",  ← 注意是 "jwt"，不是 "object"
  "warning": null
}
```

---

## 📊 测试清单

### Sandbox 环境测试

- [ ] 创建 Sandbox 测试账号
- [ ] 在真机上安装应用
- [ ] 执行购买流程
- [ ] 使用 Sandbox 账号登录
- [ ] 完成购买
- [ ] 检查服务器日志
- [ ] 确认 receipt 是 JWT 格式（`eyJ...`）
- [ ] 确认验证成功（JWT 签名验证）
- [ ] 确认交易记录中 `format: "jwt"`
- [ ] 确认余额正确增加

---

## ⚠️ 常见问题

### Q1: 如何确认是 Sandbox 环境？

**检查方法：**
1. 查看服务器日志中的 `receiptPreview`
2. 如果是 `eyJ...` 开头，说明是 JWT 格式（Sandbox/生产）
3. 如果是 `{"...` 开头，说明是 JSON 格式（Xcode）

### Q2: 如果 Sandbox 测试失败怎么办？

**可能原因：**
1. 网络问题（无法访问 App Store JWKS）
2. Bundle ID 不匹配
3. JWT 验证逻辑有问题

**排查步骤：**
1. 查看详细的错误日志
2. 检查 JWT 验证函数的错误信息
3. 确认 Bundle ID 配置正确

### Q3: Sandbox 会扣费吗？

**答案：** 不会！Sandbox 是测试环境，不会产生真实费用。

---

## 🎯 测试建议

### 推荐测试顺序

1. ✅ **Xcode 环境测试**（已完成）
   - 验证对象格式的处理
   - 验证基本验证逻辑

2. ⏭️ **Sandbox 环境测试**（建议执行）
   - 验证 JWT 格式的处理
   - 验证 JWT 签名验证
   - 最接近生产环境

3. ⏭️ **生产环境测试**（上线后）
   - 真实用户购买
   - 监控验证结果

---

## 📝 测试报告模板

```
测试日期：2025-XX-XX
测试环境：Sandbox
测试账号：sandbox-test@example.com

测试结果：
✅ Receipt 格式：JWT 字符串（eyJ...）
✅ 验证方式：JWT 签名验证
✅ 验证结果：成功
✅ 交易记录：format: "jwt", valid: true
✅ 充值功能：正常

问题记录：
[如有问题，记录 here]

结论：
[测试通过/需要修复]
```

---

## ✅ 总结

### 当前状态

- ✅ Xcode 环境测试：通过
- ⚠️ Sandbox 环境测试：待测试

### 建议

**强烈建议测试 Sandbox 环境**，因为：
1. 生产环境使用 JWT 格式
2. 验证逻辑不同（JWT 签名验证）
3. 确保功能完整
4. 避免上线后出现问题

### 如果时间紧急

如果时间紧急，可以：
1. 先上线（当前代码已支持两种格式）
2. 上线后监控验证结果
3. 如有问题及时修复

但**建议先测试 Sandbox 环境**，确保 JWT 验证正常工作。

