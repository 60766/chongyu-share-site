# 虫遇 - 角色管理指南

本文档提供了关于如何在"虫遇"应用中管理角色和头像的完整指南。

## 目录

1. [角色系统概述](#角色系统概述)
2. [角色数据结构](#角色数据结构)
3. [头像管理](#头像管理)
4. [添加新角色](#添加新角色)
5. [常见问题排查](#常见问题排查)

---

## 角色系统概述

"虫遇"应用中的角色系统由以下几个部分组成：

1. **角色数据**：存储在`characters.json`文件中，包含角色的基本信息、属性和关联。
2. **头像资源**：存储在`Assets.xcassets/HistoricalFigures`目录中，每个角色有自己的`.imageset`。
3. **角色服务**：`CharacterAvatarService`负责头像的获取和管理，`CopyHistoricalFigureImages`确保头像在运行时可用。
4. **UI组件**：如`HistoricalFigureAvatarView`负责显示角色头像。

所有这些部分必须保持一致，特别是角色ID必须在整个系统中统一。

---

## 角色数据结构

角色数据存储在`虫遇/Resources/characters.json`文件中，每个角色的基本结构如下：

```json
{
  "id": "kongzi",          // 角色唯一ID，必须与头像文件名一致
  "name": "孔子",          // 角色显示名称
  "type": "historical",    // 角色类型
  "subtype": "philosopher", // 角色子类型
  "era": "古代",           // 角色所属时代
  "primaryField": "哲学家", // 角色主要领域
  "briefDescription": "中国古代思想家、教育家", // 角色简介
  "avatarName": "kongzi",  // 头像文件名，应与ID一致
  "region": "中国",        // 角色所属地区
  "contentAffinities": {   // 内容亲和度
    "古潮新语": 0.9,
    "穿越吐槽": 0.7,
    "日常心情": 0.8,
    "虫洞共鸣": 0.8,
    "时空记事": 0.9
  },
  "relatedCharacterIds": ["plato", "aristotle", "socrates"] // 相关角色
}
```

**重要字段**：
- `id`：角色的唯一标识符，必须是小写英文，用于系统内部识别。
- `name`：角色的显示名称，通常是中文名。
- `avatarName`：头像文件名，应与`id`保持一致。

---

## 头像管理

### 头像文件结构

头像文件应存储在以下位置：
```
虫遇/Assets.xcassets/HistoricalFigures/[角色ID].imageset/[角色ID].png
```

例如，孔子的头像路径为：
```
虫遇/Assets.xcassets/HistoricalFigures/kongzi.imageset/kongzi.png
```

### 头像加载流程

1. `CharacterAvatarService.getAvatarName(for:)`根据角色ID返回标准化的ID。
2. `HistoricalFigureAvatarView`尝试从多个位置加载头像图片。
3. 如果找不到图片，显示带有角色首字母的占位图。

### 批量生成头像

使用`generate_placeholder_avatars.sh`脚本可以为所有角色生成占位头像：

```bash
chmod +x generate_placeholder_avatars.sh
./generate_placeholder_avatars.sh
```

---

## 添加新角色

添加新角色需要以下步骤：

1. **更新characters.json**：
   - 在`characters.json`中添加新角色的数据。
   - 确保`id`和`avatarName`字段使用相同的值。

2. **添加头像**：
   - 准备角色头像图片（PNG格式，建议512x512像素）。
   - 在Xcode中创建新的Image Set：`Assets.xcassets/HistoricalFigures/[角色ID].imageset`。
   - 将图片添加到Image Set中。

3. **更新CharacterAvatarService**：
   - 在`CharacterAvatarService.swift`的`knownCharacters`数组中添加新角色ID。

4. **重新编译运行**：
   - 重新编译应用并验证新角色是否显示正确。

详细步骤请参考[如何添加角色头像](README_如何添加角色头像.md)。

---

## 常见问题排查

### 头像不显示

1. **检查角色ID一致性**：
   - `characters.json`中的`id`
   - `CharacterAvatarService`的`knownCharacters`数组
   - 头像文件名

2. **检查资源文件**：
   - 确认头像文件已添加到Xcode项目中
   - 确认头像文件包含在目标应用中

3. **检查控制台日志**：
   - 查看与`CharacterAvatarService`和`HistoricalFigureAvatarView`相关的日志
   - 特别注意"尝试从文件系统加载头像"相关的日志

### 角色名称显示不正确

1. **检查characters.json**：
   - 确认角色的`name`字段设置正确

2. **检查本地化**：
   - 如果使用了本地化，检查本地化文件中的角色名称

---

通过遵循本指南，您可以确保"虫遇"应用中的角色系统运行正常，并且可以轻松添加新角色。 