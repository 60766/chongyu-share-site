# App Store 截图尺寸指南

## 📱 需要的截图尺寸

### iPhone 6.5英寸显示屏（iPhone 11 Pro Max, iPhone XS Max等）

**要求的尺寸（任选其一）：**
- ✅ `1242 × 2688px`（竖屏）
- ✅ `2688 × 1242px`（横屏）
- ✅ `1284 × 2778px`（竖屏，iPhone 14 Pro Max等）
- ✅ `2778 × 1284px`（横屏，iPhone 14 Pro Max等）

**推荐使用：** `1242 × 2688px`（竖屏）- 最常用

---

## 🎯 方法1：从Xcode模拟器直接截取（推荐）

### 步骤1：选择正确的模拟器

1. 打开Xcode
2. 选择模拟器：**iPhone 15 Pro Max** 或 **iPhone 14 Pro Max**
   - 这些模拟器的分辨率正好是 `1242 × 2688px`

### 步骤2：运行应用并截取

1. 在模拟器中运行你的应用
2. 导航到要截图的页面
3. 使用快捷键截取：
   - **macOS**: `Cmd + S`（保存到桌面）
   - 或者：`Cmd + Shift + 4`，然后点击模拟器窗口

### 步骤3：验证尺寸

1. 找到保存的截图文件
2. 右键点击 → **显示简介**（Get Info）
3. 查看尺寸是否为 `1242 × 2688px`

---

## 🎯 方法2：从真机截取并调整

### 步骤1：从iPhone截取

1. 在iPhone上打开应用
2. 导航到要截图的页面
3. 按 **电源键 + 音量上键** 截取
4. 截图会保存到相册

### 步骤2：调整尺寸

**使用macOS预览（Preview）：**

1. 打开截图（双击图片）
2. 点击菜单栏 **工具** → **调整大小**（Tools → Adjust Size）
3. 取消勾选 **按比例缩放**（Scale proportionally）
4. 设置尺寸：
   - **宽度**: `1242` 像素
   - **高度**: `2688` 像素
5. 点击 **确定**
6. 保存文件（Cmd + S）

**使用在线工具：**

- [ResizeImage.net](https://resizeimage.net/)
- [ILoveIMG](https://www.iloveimg.com/resize-image)
- 上传图片 → 设置尺寸 `1242 × 2688` → 下载

---

## 🎯 方法3：使用Sketch/Figma/Photoshop调整

### 使用Sketch

1. 打开截图
2. 选择图片
3. 在右侧面板设置：
   - **宽度**: `1242px`
   - **高度**: `2688px`
4. 导出为PNG

### 使用Figma

1. 导入截图
2. 选择图片
3. 在右侧面板设置：
   - **宽度**: `1242px`
   - **高度**: `2688px`
4. 导出为PNG

### 使用Photoshop

1. 打开截图
2. **图像** → **图像大小**（Image → Image Size）
3. 取消勾选 **约束比例**（Constrain Proportions）
4. 设置：
   - **宽度**: `1242` 像素
   - **高度**: `2688` 像素
5. 点击 **确定**
6. **文件** → **导出** → **导出为**（File → Export → Export As）

---

## 🎯 方法4：使用命令行工具（快速批量处理）

### 使用sips（macOS自带）

```bash
# 调整单张图片
sips -z 2688 1242 input.png --out output.png

# 批量调整（当前目录所有PNG）
for file in *.png; do
    sips -z 2688 1242 "$file" --out "resized_$file"
done
```

### 使用ImageMagick（需要安装）

```bash
# 安装ImageMagick
brew install imagemagick

# 调整单张图片
convert input.png -resize 1242x2688! output.png

# 批量调整
for file in *.png; do
    convert "$file" -resize 1242x2688! "resized_$file"
done
```

---

## 📋 推荐工作流程

### 方案A：从模拟器直接截取（最简单）

1. ✅ 在Xcode中选择 **iPhone 15 Pro Max** 模拟器
2. ✅ 运行应用
3. ✅ 导航到要截图的页面
4. ✅ 按 `Cmd + S` 保存截图
5. ✅ 截图自动是 `1242 × 2688px`，无需调整

### 方案B：从真机截取后调整

1. ✅ 在iPhone上截取
2. ✅ 使用macOS预览调整尺寸
3. ✅ 或使用在线工具调整

---

## ⚠️ 常见问题

### Q1: 为什么我的截图尺寸不对？

**可能原因：**
- 使用了错误的模拟器（如iPhone SE）
- 从真机截取但没调整尺寸
- 截图时模拟器窗口被缩放

**解决方法：**
- 使用 **iPhone 15 Pro Max** 或 **iPhone 14 Pro Max** 模拟器
- 确保模拟器窗口是100%大小（不要缩放）

### Q2: 如何确保模拟器是正确尺寸？

1. 打开模拟器
2. **窗口** → **物理尺寸**（Window → Physical Size）
3. 或使用快捷键：`Cmd + 1`（100%大小）

### Q3: 截图有黑边怎么办？

**原因：** 模拟器窗口被缩放或截图时包含了窗口边框

**解决方法：**
- 确保模拟器窗口是100%大小
- 使用 `Cmd + S` 直接保存，不要用截图工具

### Q4: 可以裁剪截图吗？

**可以，但要注意：**
- 必须保持 `1242 × 2688px` 的最终尺寸
- 可以裁剪内容，但最终输出必须是这个尺寸

---

## ✅ 检查清单

在提交前，请确认：

- [ ] 截图尺寸是 `1242 × 2688px`（或其他要求的尺寸）
- [ ] 所有截图都是同一尺寸
- [ ] 截图清晰，没有模糊
- [ ] 截图没有黑边
- [ ] 截图内容完整，没有被裁剪

---

## 🎯 快速验证方法

### 在macOS上验证尺寸：

1. 选择截图文件
2. 按 `Cmd + I`（显示简介）
3. 查看 **尺寸**（Dimensions）
4. 确认是 `1242 × 2688` 像素

### 使用命令行验证：

```bash
# 查看图片尺寸
sips -g pixelWidth -g pixelHeight screenshot.png
```

应该显示：
```
pixelWidth: 1242
pixelHeight: 2688
```

---

## 📝 推荐操作步骤

### 最简单的方法：

1. **打开Xcode**
2. **选择模拟器：iPhone 15 Pro Max**
3. **运行应用**
4. **导航到要截图的页面**
5. **按 `Cmd + S` 保存截图**
6. **完成！** ✅ 截图自动是正确尺寸

---

## 💡 提示

- ✅ **推荐使用模拟器截取** - 最简单，自动正确尺寸
- ✅ **使用iPhone 15 Pro Max模拟器** - 分辨率正好是 `1242 × 2688px`
- ✅ **一次截取所有需要的页面** - 避免重复操作
- ✅ **保存到固定文件夹** - 方便管理和上传

---

**按照以上步骤操作，你的截图尺寸就会正确了！** 🎯

