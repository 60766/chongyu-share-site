# 真机测试 IAP Sandbox 购买 - 手把手教程

## 📱 准备工作

### 1. 确保你的 iPhone/iPad 已连接
- 用 USB 线连接 iPhone/iPad 到 Mac
- 在 iPhone/iPad 上：**设置 → 通用 → VPN与设备管理**，信任你的 Mac（如果提示）

### 2. 在 Xcode 中选择真机设备
- 打开 Xcode
- 点击顶部设备选择器（现在显示的是模拟器名称）
- 选择你的 iPhone/iPad 设备

---

## 🔧 步骤 1：配置真机调试

### 1.1 检查开发者账号
- 在 Xcode 中：**Xcode → Settings → Accounts**
- 确保已登录你的 Apple Developer 账号
- 如果没有，点击左下角 `+` 添加账号

### 1.2 配置项目签名
- 在 Xcode 项目导航器中，选择项目（最顶部的 "虫遇"）
- 选择 **TARGETS → 虫遇**
- 点击 **Signing & Capabilities** 标签
- 确保 **Automatically manage signing** 已勾选
- 在 **Team** 下拉菜单中选择你的开发者账号
- Xcode 会自动生成 Provisioning Profile

### 1.3 检查 Bundle Identifier
- 确保 Bundle Identifier 是：`com.lishilong.chongyu`
- 如果不同，需要修改为这个（因为后端验证需要匹配）

---

## 📲 步骤 2：在真机上运行应用

### 2.1 编译并运行
- 在 Xcode 中，点击左上角的 **运行按钮**（▶️）或按 `Cmd + R`
- 第一次在真机上运行可能需要几分钟
- 如果提示需要信任开发者：
  - 在 iPhone/iPad 上：**设置 → 通用 → VPN与设备管理**
  - 找到你的开发者证书，点击 **信任**

### 2.2 确认应用已安装
- 应用应该已经安装到你的 iPhone/iPad 上
- 打开应用，确保可以正常使用

---

## 🚪 步骤 3：退出 Apple ID（重要！）

### 3.1 退出设备上的 Apple ID
1. 在 iPhone/iPad 上打开 **设置**
2. 点击最顶部的 **Apple ID**（你的名字和头像）
3. 滚动到底部，点击 **退出登录**
4. 选择 **保留在我的 iPhone 上**（保留数据）
5. 输入 Apple ID 密码确认退出

### 3.2 确认已退出
- 退出后，**设置** 顶部应该显示 **登录 iPhone** 而不是你的名字
- 这是关键步骤！只有退出 Apple ID，系统才会在购买时弹出登录界面

---

## 🧪 步骤 4：设置 Sandbox 测试账号

### 4.1 在 App Store Connect 创建 Sandbox 测试员
1. 打开浏览器，访问：https://appstoreconnect.apple.com
2. 登录你的开发者账号
3. 进入 **用户和访问** → **沙盒技术测试员**
4. 点击 **+** 添加测试员
5. 填写信息：
   - **名字**：Shilong（任意）
   - **姓氏**：Test（任意）
   - **邮箱**：`shilong@tester.com`（必须是真实邮箱，会收到验证码）
   - **密码**：设置一个密码（记住它！）
   - **国家/地区**：选择中国
6. 点击 **创建**
7. 检查邮箱，点击验证链接激活账号

### 4.2 在应用中输入 Sandbox 账号
1. 打开应用，进入 **充值页面**
2. 在 **购买须知** 区域，找到 **"输入 Sandbox 测试账号"** 按钮
3. 点击按钮，输入：
   - **邮箱**：`shilong@tester.com`
   - **密码**：你刚才设置的密码
4. 点击 **保存**

---

## 💰 步骤 5：执行 Sandbox 购买测试

### 5.1 启用 Sandbox 模式
1. 在充值页面，找到 **"使用 Sandbox 账号购买"** 开关
2. **开启** 这个开关

### 5.2 开始购买
1. 选择一个充值套餐（例如：¥6 = 100能量）
2. 点击 **购买** 按钮
3. 系统会弹出提示，告诉你需要退出 Apple ID
4. 点击 **"打开设置退出 Apple ID"**（如果还没退出）
5. 或者点击 **"已退出，继续购买"**

### 5.3 登录 Sandbox 账号
1. 点击购买后，系统会弹出 **Apple ID 登录界面**
2. 在登录界面，**不要**使用设备上已保存的账号
3. 点击 **"使用其他账号"** 或直接输入
4. 输入 Sandbox 账号：
   - **邮箱**：`shilong@tester.com`
   - **密码**：你设置的密码
5. 如果提示需要验证码，检查邮箱并输入验证码
6. 完成登录

### 5.4 完成购买
1. 登录成功后，系统会继续购买流程
2. 确认购买（Sandbox 环境不会真的扣费）
3. 等待购买完成

---

## ✅ 步骤 6：验证购买结果

### 6.1 检查应用内余额
- 购买成功后，应用应该显示余额已增加
- 检查充值页面显示的虫洞币数量

### 6.2 检查服务器日志
运行以下命令检查验证结果：

```bash
./check_latest_iap.sh
```

你应该看到：
- `[IAP Verify] ✅ 交易验证成功`（JWT 格式）
- 或者 `environment: 'Sandbox'`
- 而不是 `environment: 'Xcode'`

---

## 🔍 常见问题

### Q1: 点击购买后没有弹出登录界面？
**A:** 确保你已经：
- ✅ 完全退出了设备上的 Apple ID
- ✅ 在真机上运行（不是模拟器）
- ✅ 没有使用 StoreKit Configuration 文件

### Q2: 提示"无法连接到 iTunes Store"？
**A:** 
- 检查网络连接
- 确保设备可以访问互联网
- 尝试重启应用

### Q3: 登录时提示"此 Apple ID 尚未在 App Store 中使用过"？
**A:** 
- 这是正常的！Sandbox 账号是独立的测试账号
- 直接点击 **"检查"** 或 **"继续"** 即可

### Q4: 需要验证码但收不到？
**A:**
- 检查邮箱（包括垃圾邮件文件夹）
- 确保在 App Store Connect 中创建的邮箱地址正确
- 可以重新发送验证码

### Q5: 购买后余额没有增加？
**A:**
- 检查服务器日志，看是否有错误
- 确认网络连接正常
- 尝试刷新余额

---

## 📊 验证成功的标志

如果一切正常，你应该看到：

1. ✅ 应用内余额增加
2. ✅ 服务器日志显示 JWT 格式验证成功
3. ✅ 日志中显示 `environment: 'Sandbox'` 而不是 `'Xcode'`
4. ✅ 没有验证警告或错误

---

## 🎉 完成！

如果看到以上所有标志，说明 Sandbox 环境测试成功！

现在你的 IAP 验证系统已经可以：
- ✅ 验证 Xcode 环境的购买（JSON 对象格式）
- ✅ 验证 Sandbox 环境的购买（JWT 格式）
- ✅ 验证生产环境的购买（JWT 格式，上线后）

---

## 💡 提示

- Sandbox 购买**不会真的扣费**，可以反复测试
- 每次测试后，可以在 App Store Connect 中查看测试购买记录
- 生产环境上线后，验证逻辑会自动切换到生产环境

