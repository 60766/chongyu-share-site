# 🔍 HTTPS 设置 - 当前状态检查

你现在已经登录到服务器了！让我们先检查一下当前状态。

## 📋 第一步：检查后端服务是否运行

在服务器上执行：

```bash
curl -I http://127.0.0.1:3000
```

**期望看到**: `HTTP/1.1 200 OK` 或类似响应

**如果没有响应**，说明后端服务没有运行，需要先启动后端。

---

## 📋 第二步：检查是否已经安装了 Caddy

```bash
caddy version
```

**如果显示版本号**，说明已经安装了。

**如果显示 `command not found`**，说明还没有安装，需要运行脚本。

---

## 📋 第三步：检查 Caddy 服务状态

```bash
sudo systemctl status caddy
```

**如果显示 `Active: active (running)`**，说明 Caddy 已经在运行。

**如果显示 `inactive` 或 `failed`**，需要启动或修复。

---

## 📋 第四步：检查端口占用

```bash
sudo lsof -i:80 -i:443
```

**如果有输出**，说明端口被占用。

**如果没有输出**，说明端口空闲。

---

## 📋 第五步：检查阿里云安全组（重要！）

这一步必须在**浏览器**中完成，不能在服务器上操作：

1. 打开浏览器，访问：https://ecs.console.aliyun.com/
2. 登录你的阿里云账号
3. 进入 **ECS** → **实例**
4. 找到你的服务器（IP: 121.40.184.29）
5. 点击 **安全组** → **配置规则**
6. 检查是否有 **80** 和 **443** 端口的入方向规则
7. 如果没有，需要添加（参考下面的详细步骤）

---

## 🚀 如果还没有运行脚本，现在运行：

```bash
chmod +x /root/setup_https_caddy.sh
/root/setup_https_caddy.sh
```

脚本会自动完成所有设置。

---

## ⚠️ 重要提醒

在运行脚本之前，**必须先完成阿里云安全组设置**，否则证书申请会失败！

